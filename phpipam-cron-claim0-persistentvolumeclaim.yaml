apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: phpipam-cron-claim0
  name: phpipam-cron-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
