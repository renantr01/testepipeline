apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: phpipam-web-claim0
  name: phpipam-web-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
